#!/bin/sh

# if [ "$WAYLAND_DISPLAY" ]; then
	/lib/discord-canary/Discord --enable-features=UseOzonePlatform --ozone-platform=wayland --enable-webrtc-pipewire-capturer /lib/discord-canary/resources/app.asar
# else
# 	/lib/discord-canary/Discord /lib/discord-canary/resources/app.asar
# fi
