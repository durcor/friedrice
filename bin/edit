#!/bin/sh

if [ ! "$1" ]; then
    if [ "$VIMRUNTIME" ]; then
        $EDITOR -c :terminal
    else
        nvim
    fi
elif [ -O "$1" ] || [ ! -f "$1" ]; then
    $EDITOR "$1"
else
    sudoedit "$1"
fi
