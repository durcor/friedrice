#!/bin/sh

ping -c 1 1.1.1.1 || exit

if ! ls /etc/hosts.mine
then
    cp /etc/hosts /etc/hosts.mine
else
    cp /etc/hosts.mine /etc/hosts
fi
curl https://raw.githubusercontent.com/StevenBlack/hosts/master/hosts >> /etc/hosts
